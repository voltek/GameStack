# Must match targetSdk in app/build.gradle.kts. Pinned here rather than per-class
# so a screen test that omits @Config cannot silently run on another API — but
# nothing enforces the match, so bump this when targetSdk moves.
sdk=36
