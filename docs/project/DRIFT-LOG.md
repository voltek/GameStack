# DRIFT-LOG — GameStack

> Every time the code and the documentation disagreed, and in which direction it
> was resolved — the code was fixed, or the document was updated. That is exactly
> what CLAUDE.md's Self-Healing Loop requires be recorded, and this file is where
> it goes.
>
> **A decision that is born or changes does not belong here: that is a new ADR
> under `docs/project/decisions/`.** This file is written in the past tense and
> is unbounded — it grows with every incident. Its neighbour
> `DRIFT-CHECKLIST.md` is the imperative list of violation detectors, and grows
> only when a new check is added.
>
> **Rows are ordered newest first**, and a new one goes on top. The order is the
> only thing a reader can use to see a pattern repeat, so it has to be true.

| Date | Item | Resolution (code fixed / doc updated) |
|---|---|---|
| 2026-09-02 | The checklist's header states that **every item is written so that "no" is the healthy answer**, and item 3 of *Documentation integrity* was the only one phrased the other way round: it asked whether the Available Skills list *matched* `.claude/skills/` and the Spec, where a healthy repository answers "yes". Found on 2026-09-01 while running the five *Documentation integrity* detectors as a set, and deliberately deferred to this change rather than fixed there — see the 2026-09-01 row ("The Spec's copy of the Skills list named twelve procedures") | Doc updated — the item is rewritten as a violation detector ("does the list *differ* from …"), so "no" is the healthy answer across every item without exception. The question it asks is unchanged; only its polarity is. Fixed here and not on 2026-09-01 because this is the change that annotates every item with `Verified by:`: an item read backwards by a person is a nuisance, but one read backwards by the automated checker those annotations are the input for is a false negative that reports health. **No ADR**: the convention already existed in the header and this restores an item to it, which is the boundary the Self-Healing Loop draws between an incident and a decision |
| 2026-09-01 | ADR 0008 bans `Rebase and merge`, and the button was still enabled in the repository settings — the ban lived entirely in prose with the violation one click away. Surfaced by writing that ADR's `Enforced by:`, which is the field's whole job. Closing it then exposed a second and larger problem: with the button gone, `Enforced by: none (prose only …)` understates what is true, and **nothing in the system can correct it**. The immutability rule permits editing only the `Status:` line, and the field takes exactly one value, so it could not express "the merge button is policed, the branch-update rule and the PR-size alarm are not" even if it were editable | Setting changed — `allow_rebase_merge=false`. `Create a merge commit` and the `Squash and merge` fallback remain, so CLAUDE.md's merge-strategy rule is unchanged and now has a mechanism behind it instead of prose alone. **ADR 0008 is deliberately left untouched, `Enforced by:` included**: under the rule as it stands that field is a dated record, and editing it would be exactly the silent rewrite of an accepted ADR that immutability exists to prevent. The gap that leaves is real and is recorded here rather than papered over — the map `grep "^Enforced by:"` produces now understates enforcement in this ADR, and in ADR 0015, whose "every thread is answered and then resolved" clause `main`'s branch protection has been enforcing all along (`required_conversation_resolution=true`). Making the field live was drafted in this same branch and **withdrawn**: a `/code-review` round showed that declaring it live obliges a sweep of all twenty ADRs, a detector for the external GitHub state it would then assert, and a change to TOOLING-BACKLOG's *when an item ships* list — and that none of those can be done honestly while the field holds a single value. Two ADRs have now hit that limit (0010, where `lint` covers one clause of two; 0008 here, one rule of three), and the withdrawn fix introduced its own next defect in the same place, which CLAUDE.md counts double. **So the rule of three applies and the field gets redesigned, not patched a fourth time.** Deferred deliberately: the redesign is a plan-level decision, specified for a session that can take it properly rather than improvised at the end of this one |
| 2026-09-01 | Backfilling the nineteen ADRs put `decisions/README.md`'s own rules against every real case for the first time, and two of them came up short on the same ADR. The portrait lock is product scope: it lives in the Spec and not in CLAUDE.md, so the `Enforced by:` vocabulary had no value that could address it without pointing at a document that does not carry the rule, and the numbering tie-break — "by the order of the CLAUDE.md section the rule belongs to" — did not determine its position at all. This is the ordinary way a rule written against the cases in view comes up short: not by being wrong, but by never having met the full set | Doc updated. `Enforced by:` now reads `none (prose only — <document> § <section>)` and names whichever governing document holds the rule; the tie-break now says that an ADR whose rule lives outside CLAUDE.md sorts after every ADR of that date whose rule does not. **No ADR**: both complete an existing rule rather than creating or changing one, which is the boundary the Self-Healing Loop draws. The same pass repointed the README's worked example at the real ADR 0014 and its real superseded predecessor, since the numbers it used had been assigned to unrelated decisions and its quoted row address did not resolve |
| 2026-09-01 | The Spec's copy of the Skills list named twelve procedures while `.claude/skills/` and CLAUDE.md's authoritative list both held fourteen: `verify-on-device` and `ship-a-branch` were missing. Both are Shipping-group Skills that landed later than the rest, and the Spec's line was never updated with them. The Spec's own sentence states that if the two lists disagree that is drift, so the document named its own failure mode and the detector for it was already written correctly in DRIFT-CHECKLIST; it had simply never been run | Doc updated — the Spec now names all fourteen. CLAUDE.md was already correct and stays the authoritative list, so nothing changed there. **No ADR**: this restores an existing rule rather than creating or changing one, which is the boundary the Self-Healing Loop now draws. Found by running the five *Documentation integrity* detectors as a set against the ADR-migration branch — the first time they had been run that way, which is the actual lesson: a correct detector nobody runs catches nothing. The same pass found that item 3 of the forty-five is phrased so that "yes" is the healthy answer, inverting the checklist's stated convention; that is resolved with the checklist classification work, not here |
| 2026-08-25 | `git add -A` recorded `.claude/worktrees/agent-<id>` as a `160000` gitlink and committed it. Agent worktrees are checkouts of this repo that live inside it, and nothing ignored them — so every `add -A` run while one exists silently stages a submodule pointer. Caught by reading `git show --stat` before pushing, which is the only reason it did not ship | Code fixed — `/.claude/worktrees/` added to `.gitignore`, scoped to that one directory so `.claude/skills/` stays versioned. Deliberately not a new rule in CLAUDE.md: *Read `git status` in full before committing* already covers the habit, and closing the hole mechanically means the habit no longer has to catch it |
| 2026-08-25 | TOOLING-BACKLOG's own rule sends a shipped item's *rule* to CLAUDE.md, its *procedure* to a Skill, and its *incident* to this log — and nothing anywhere holds the **inventory**: what the harness is made of right now, and which shapes were rejected reaching it. So every time a tooling item shipped, the answer to "what does this repository actually use, and why not the obvious alternative" was distributed across four files and a `git log`, recoverable only by whoever already knew. Surfaced by the need to reproduce this setup in a future project, which is the case the split serves worst | Doc updated, and the rule that produced the gap fixed in the same change. `docs/project/TOOLING.md` holds one entry per piece of the harness — what it is, which manual step it replaced, the file that configures it, and the shapes ruled out with the reason. TOOLING-BACKLOG's *when an item ships* clause now names it as the fourth destination, so the gap cannot silently reopen on the next item. Deliberately **not** referenced from CLAUDE.md: no rule there is incomplete without it, and a pointer is paid for on every turn. Read on demand instead, via the README. The boundary that keeps it from becoming the second changelog the backlog warns about: an inventory states what *is*, in the present tense, while a changelog states what *happened* — `git log` keeps the second job |
| 2026-08-25 | `e48eb0d` ("chore(ci): run the gate on GitHub Actions") changed `gradlew`'s file mode from `100644` to `100755` — confirmed via `git ls-files -s gradlew` and `git log --follow -p -- gradlew`, which shows `old mode 100644` / `new mode 100755` on that commit — but the commit message never mentions it, and the change was recorded in neither `TOOLING.md` nor this log (checked both with `grep`). Without the executable bit, `.github/workflows/gate.yml`'s `./gradlew build test lint --continue` step would fail on the Linux runner with `Permission denied`: a clone preserves whatever mode git committed, and the wrapper script is invoked directly rather than via `sh gradlew` | Doc updated only — the code is already correct (`100755` is the right mode, and stays). The commit itself is not rewritten to add this: CLAUDE.md's Git workflow does not permit rewriting a merged commit's history to retrofit an explanation, and that is exactly what this Resolution log exists to hold instead |
| 2026-08-24 | Three related gaps surfaced while auditing the git workflow end-to-end: CLAUDE.md's ask-before-acting rule covered `git push` and opening a PR but never the merge itself; `ship-a-branch` had no step to catch a branch whose name no longer matched its final scope before push (the branch is named at the start, from a guess made before the work existed, and nothing ever revisits it); and the PR body's `Why` section had no length target, so it ranged from 2 sentences (PR #3) to 4 paragraphs (PR #15) with nothing at the point of writing to say which is right | Doc updated. CLAUDE.md's Tier 1 line now reads "ask before `git push`, before opening a PR, and before merging it"; `ship-a-branch` step 6 carries a matching reminder that merge needs its own explicit yes, since asking earlier for push/PR does not cover it. Step 2 gained a check — does the branch name still describe what it now contains — with the rename recipe, run before step 3's reviews so a stale name never reaches the PR title. Step 4 states `Why` defaults to one paragraph (three to five sentences), expanding only for real nuance a reviewer can't get from the diff; `pull_request_template.md` carries a matching inline comment so the target is visible while writing, not only inside the skill. Considered and rejected: putting the merge-approval rule in a Skill instead of CLAUDE.md (it needs to hold even when no Skill is active — the same reasoning as the existing push/PR rule already there) and a new branch-creation Skill or a `new-feature` step for the naming check (most branches on this repo are `fix`/`chore`/`docs`, which never invoke `new-feature`, and a wrong name costs nothing before push — the step 2 check already catches it at the one point that matters, for every branch regardless of what built it) |
| 2026-08-24 | Installing the Claude GitHub App (PR #18) landed `claude-code-review.yml` triggering on `pull_request: [opened, ...]`, which contradicts `ship-a-branch` step 3 — every `/code-review` round finishes *before* a PR exists, so a review fired at open reads a branch whose local rounds may still be landing and comments on findings already fixed. A `labeled`-trigger fix was drafted first (request the bot once via a `claude-review` label), but on review the project decided it does not want an automated Claude reviewer on GitHub *at all* for now — code review stays internal-only (`/code-review`, run by the human). Found in the same pass as an unrelated second issue of the same shape: `git fetch --prune` dropped eleven remote-tracking refs GitHub had already deleted, plus one local branch nobody had removed — both are the same asymmetry, GitHub automated one side of a step and the repo never said who owns the other | Two different resolutions for two different findings. (1) The bot: rather than retrigger it differently, `claude-code-review.yml` was disabled outright via `gh workflow disable "Claude Code Review"` — reversible with `gh workflow enable`, and free while off, since Actions billing is by executed minutes, not by a workflow file's presence. `ship-a-branch` step 3 and the workflow file itself were left exactly as they read before PR #18's drift, `@codex review` included — Codex is a separate, already-external bot and stays untouched. If a manual, on-demand Claude review is wanted later: trigger on a PR comment containing `/claude-code-review` (not an `@claude` mention) — the general assistant in `claude.yml` already reacts to any comment containing `@claude`, and `contains()` is a substring check, so any `@claude`-prefixed phrase double-fires both workflows on the same comment. A trigger phrase with no `@claude` substring avoids that collision without touching `claude.yml`. Keep the existing plugin (`code-review@claude-code-plugins`) and inline-comment tool as-is when that day comes — only the `on:`/`if:` block needs to change from this branch's `labeled` draft. (2) The local branch cleanup: unrelated to the above, and kept. `ship-a-branch` step 6 gains the local-branch rule, `-d` and never `-D`, with the order that makes it work (`fetch --prune`, fast-forward `main`, then delete). `-d` refuses unless the tip is an ancestor of `main`, which is exactly the squash-merge case where the branch holds the only copy of its granular history — so the refusal is the safeguard and `-D` is the key that disables it. Noted as prose, the weakest rung of the ladder: the merge happens on GitHub, so no local hook can fire |
| 2026-08-24 | The gate (`build`/`test`/`lint`) existed only as prose in CLAUDE.md, enforced by whoever remembered to run it. `main` was protected but *require status checks* was off, because there was no check to require — so the strongest protection available was switched off by a missing mechanism rather than by a decision | Tooling built rather than a rule added. `.github/workflows/gate.yml` runs the three on every PR and every push to `main`, moving the gate a rung up the enforcement ladder: prose → mechanism. Three things it had to get right, each already known and each easy to get wrong: the daemon is pinned to Java 21 by `gradle/gradle-daemon-jvm.properties`, so the runner installs 21 or Gradle downloads a toolchain per job; `~/.m2/repository/org/robolectric` is cached *separately* from `~/.gradle`, because Robolectric fetches its 204MB SDK jar at test runtime through its own Maven client (the `testImplementation` shortcut was tried and reverted on 2026-08-23); and no IGDB credentials are wired in, since `local.properties` is optional and falls back to empty — a secret added "just in case" would have been a real one leaked into a workflow for nothing. TOOLING-BACKLOG's *CI on GitHub Actions* entry leaves that file per its own rule, its pointer in `libs.versions.toml` repointed at the workflow. *Require status checks* is turned on after the first green run, not before — a required check that has never reported blocks every merge. Three things were hardened before the first run rather than after it: `--continue`, so one run reports every independent failure instead of costing a push per verdict; `timeout-minutes: 30` against a 360-minute default; and `actions/cache` split into `restore` + `save`, because the combined action saves in a post step that is skipped when the job fails, which is exactly when the next run comes soonest. That save is guarded with `!cancelled()` and not `always()`, so a run killed by the concurrency group cannot persist a half-written jar under the primary key, where it would read as an exact hit and never be overwritten |
| 2026-08-23 | Nothing said whether the app supports landscape. Every screen was designed portrait and none had ever been checked rotated, so the app shipped an orientation it had not been designed or tested for — and a `verticalScroll` had already been added to `MessageState` *for* landscape, which made the gap look addressed when it was not | Code fixed and Spec updated — the manifest locks portrait and the Spec records the deferral. Verified by forcing `user_rotation=1` on emulator-5554: the app stayed portrait at 1080x2424. The decision, its reason and its two carve-outs: [ADR 0019](decisions/0019-the-app-is-portrait-only.md) |
| 2026-08-23 | `design/search/search.html` carried a "Search Results" heading that its own PNG never showed — hidden by the same `sticky` overlap as the Filters button — and that the implementation does not render. Left open as a plan-level call when the mockup work surfaced it | Resolved by the human: **it leaves the design**. Heading deleted, PNG regenerated. The Search mockup now shows exactly what the app renders, so the design and the code agree with nothing left to reconcile. Note the shape of this one: the markup was invisible for long enough that a Resolution row was written from the render and read as if it described the file — dead markup does not merely sit there, it misleads |
| 2026-08-23 | The force-push window was reopened to closing at merge on the argument that an orphaned review thread loses little. That argument was reasoning, not evidence, and it carried a real risk nobody had checked: `main` requires conversation resolution, so a thread that could not be resolved after a rewrite would leave the merge button permanently blocked | Tested rather than argued, on PR #12 — the human left an inline comment, the commented line was then rewritten and force-pushed. Result: the thread keeps its text and `path`, keeps `originalLine`, loses `line` (the live anchor becomes null) and is flagged `isOutdated`; it still accepts replies and still resolves, and resolving it moved the PR from `BLOCKED` to `CLEAN`. So the only loss is the diff anchor — the presentation, not the conversation — and there is no deadlock. The rule stands, and `ship-a-branch` now records the measurement instead of the assumption. Worth keeping as the shape of the answer: the doubt was empirical, so it was settled empirically, at the cost of one throwaway comment |
| 2026-08-23 | The refresh-failure banner shipped, was verified on device and merged, and had no design reference at all: `search.html` still showed the state it replaced. Nothing in the harness required a mockup for a new state, so the gap was invisible until someone asked what the screen is supposed to look like | Asset added — `design/search/search-refresh-error.html` and `.png`, with the recipe in `design/README.md`, which also gains the trap this cost five renders to find: a `sticky` element with a `top` offset displaces itself downward and paints over whatever follows, so the banner was present in `--dump-dom` and invisible in the screenshot. Drop the `sticky` and give `main` a `pt-[72px]` for a static export. The rule that a new state ships its mockup: [ADR 0012](decisions/0012-a-new-screen-state-ships-its-mockup.md) |
| 2026-08-23 | Building that mockup exposed that `design/search/search.html` **contains** a "Search Results" heading and a "Filters" button that its own PNG does not show — the same `sticky` overlap that hid the banner paints over them. So the earlier row saying "the approved mockup shows none" was right about the rendered asset and wrong about the file, and the real defect was markup that exists, is invisible, and had already misled one reading | Asset fixed — the Filters button is deleted from both mockups by explicit human decision: filters are deferred in the Spec, and a control nobody can see is exactly how a deferred feature leaks back in. `search-refresh-error.html` also drops the heading so it shows precisely what the app renders; `search.html` keeps it, still invisible, pending a plan-level call on whether the heading joins the code or leaves the design. Both PNGs regenerated, since removing markup that occupied space in flow shifts the layout even though it was painted over |
| 2026-08-23 | The measurement justifying the new Robolectric rule was wrong in CLAUDE.md, this log and TOOLING-BACKLOG: "~4s for eight screen tests against ~1.5s for the other 27". Re-measured, the shape was also wrong: the cost scales at roughly a second per screen test rather than amortising as a per-class startup, and Robolectric is most of `testDebugUnitTest`. The old figure had been carried forward after more tests were added, and "27" was one test class mistaken for the whole suite. A recorded bound whose number is wrong is worse than none — the next reader sees 8.5s against a recorded 4s and cannot tell whether the suite regressed or the record lied | Doc updated in all three places, the mechanism (scales, does not amortise) replacing the wrong figures. The Gradle-task split is no longer "dropped as complexity bought for nothing" — it is TOOLING-BACKLOG item 4, ranked above the post-edit hook, since that hook's premise is a quick loop the split is what preserves. The corrected rule and its event-shaped trigger: [ADR 0016](decisions/0016-robolectric-only-for-screen-tests.md) |
| 2026-08-23 | Testing Stack (Tier 1) forbade Robolectric so `./gradlew test` would stay fast. The rule predated the question it was blocking: Compose screen tests under Robolectric run on the JVM in seconds, and they are the only thing that can reach Composable-level logic. Two real gaps were sitting behind it — the `rememberSingleClick` fix on the empty-state CTA had no test at all (recorded as untestable on 2026-08-22), and nothing asserted that the refresh banner declares a live region | Rule changed after explicit conversation, and narrowed rather than relaxed — [ADR 0016](decisions/0016-robolectric-only-for-screen-tests.md). The split into separate Gradle tasks was deferred on a measurement that later proved wrong, corrected in the row above. `SearchContent` is now `internal` so tests drive it with a `UiState` instead of standing up a ViewModel |
| 2026-08-23 | The first screen test found that `rememberSingleClick` swallowed the *first* click, not the second: `lastClickMillis` initialised to `0L`, which is a real `SystemClock.uptimeMillis()` value and therefore claims a click happened at boot. Any click within 700ms of boot was dropped | Code fixed — the field is now nullable, with null meaning "never clicked". Not a user-visible defect: on a device, uptime is hours by the time a screen is interactive, which is exactly why no manual pass could ever have caught it. It was caught the way it should be, in the right order: the test was written first, failed with an empty event list, and the failure named the cause. Worth recording as the first return on screen tests, on the day they were introduced |
| 2026-08-23 | Two workflow rules were producing the mess they were meant to prevent. PRs were opened before the `/code-review` rounds finished, so threads anchored to SHAs that later moved, the force-push window closed, and the PR body went stale from the day it was written. And the force-push window closing at *first review* froze branch history to protect bot threads — the `chore/screen-tests` branch reached eight commits, four of which existed only to correct earlier commits on the same branch, which CLAUDE.md already forbade with no point of enforcement | Doc updated after explicit conversation, as Tier 2 requires; `ship-a-branch` carries the procedure, including keeping commit bodies to the diagnosis of the change rather than a narration of the review. Applied immediately: the eight commits folded to three, each verified green on its own. The rules themselves: [ADR 0014](decisions/0014-the-force-push-window-closes-at-the-merge.md) |
| 2026-08-23 | "Block N" is course vocabulary — the phase numbering of the human's separate study programme — and it had leaked into CLAUDE.md, the Spec, `write-tests` and this checklist. The tell was structural: the Spec carried a whole section, and this checklist a detector, whose only job was to manage the numbering ("do not infer meaning for an undefined block number"). A rule that exists solely to cope with something that should not be there disappears when the thing is removed | Doc updated. Three buckets separated: **product** roadmap (recommendations, Light theme, LOVED, KMP) stays in the Spec under Explicitly Deferred, now named plainly instead of by block number; **repository tooling** planned but not built moved to `docs/project/TOOLING-BACKLOG.md` — harness, not product, so neither CLAUDE.md nor the Spec owns it; **the course itself** (curriculum, phase order, learning objectives) is out of the repository entirely. The Spec's "Roadmap Phases" section and every active reference are gone; rows below keep the old vocabulary because they are a record of what was true when written. A detector replaces the old one: does any document use study vocabulary rather than naming the work itself. Note that `BLOCK-4.md`, created earlier the same day, was already the wrong name — the split had been made one level too late |
| 2026-08-23 | CLAUDE.md had grown to 680 lines with no admission criterion, and four entries that did not belong had already been removed one at a time after the fact: a machine-local screenshot path, a device serial, a render recipe, and a procedure only used while closing a branch. Measured, the Git workflow section alone was 160 lines — 24% of a file injected into *every* request, and more than the entire Architecture section — while being relevant only on the few turns that close a branch | Doc updated. CLAUDE.md opens with a Tier 1 admission rule (does it survive a clone / is it a rule or a procedure / is it a rule or history), the first question being the human's, since that is the one that kept catching environment-specific entries. Two Skills created — `ship-a-branch` (update, clean, PR body, review rounds, merge strategy) and `verify-on-device` (emulator procedure) — and the corresponding CLAUDE.md sections cut to the rules plus a pointer. Block 4's design notes moved to `docs/project/BLOCK-4.md`; the `completedAt` ownership essay cut to the boundary, with the "why not a UseCase or the DAO" reasoning completed in `new-repository-impl` in the same edit. 680 → 602 lines, of which +35 is the new admission rule, so ~110 moved out; no section now exceeds 47 lines. Stopped there deliberately rather than hitting a line target — the remaining length is rules, and cutting further would cost one |
| 2026-08-23 | The merge-strategy, keep-the-messy-history and process-work decisions all rested on one fact recorded nowhere in the repo: that this project's git history is study material, not only a changelog. It lived in an agent's memory, so every decision resting on it read as arbitrary to anyone reading the repo — and the same question ("why not rebase, everyone rebases") had already been asked twice | Doc updated — the Spec gained *Why this project exists, and what it optimises for*, stating that GameStack is both a Play Store product and a deliberate case study, and listing the three engineering consequences. Placed in the Spec rather than CLAUDE.md because it is read on demand, which is the same rule the CLAUDE.md diet in the row above is enforcing. The git rules now point at it in one clause instead of re-arguing it |
| 2026-08-23 | The 2026-08-22 entry below already stated the rule — a generalizable rule lives in CLAUDE.md plus its Skill and gets a pointer in code, rationale for one non-obvious line stays in the code, never both — and it was then broken five times in one file. Measured: `SearchViewModel.kt` at 25% comment lines and `SingleClick.kt` at 35%, against 5–9% everywhere else, one sediment layer per review round. Most of the excess was *history* (which earlier version had which defect, which review found it) duplicated from the Resolution log, and some of it will be false after the next refactor — stale comments being worse than none, since they cost trust in the accurate ones too | Doc updated and code fixed — the three questions, the preference for encoding intent as a name, type or test, and the two bounds are [ADR 0018](decisions/0018-comments-carry-the-why.md). Detector added above. `SingleClick.kt` pruned from 35% to ~14% with nothing lost — every deleted line already existed in a Skill or in this log. `SearchViewModel.kt` pruned in the same branch once PR #5 merged (25% → 16%, longest block 4 lines) — deliberately not forced below the 15% alarm. What went: the history of which earlier version carried which defect, and the narration of what the operators replaced; what stayed: the two traps a correct-looking refactor would walk into (the wait must live inside `mapLatest`, `isRefreshing` must be set synchronously for `PullToRefreshBox`). Second rule this week that existed without an enforcement point, after PR size |
| 2026-08-23 | Neither CLAUDE.md nor the Spec ever said whether the MVP targets accessibility, so a fourth `/code-review` round raised a missing TalkBack live region on the new refresh banner with no basis on which to triage it — and neither the reviewer nor the agent could tell "we are not doing accessibility yet" from "we broke accessibility" | Doc updated — CLAUDE.md gained an "Accessibility — Tier 2" section and the Spec a low-priority backlog entry. The deferral, its two bounds and its expiry: [ADR 0013](decisions/0013-the-mvp-does-not-target-accessibility.md). The live region was therefore still fixed under the new rule, as un-regressing rather than as new work. **Recorded as unverified:** the next review round noted that Compose live regions fire reliably on a *text change* to an existing node, and may not announce a node that has just appeared — which is precisely this case. Settling it needs a manual TalkBack pass, which is what the accessibility backlog covers, so the fix is the right shape but the claim that the affordance is fully restored is not yet evidence. Do not cite this row as proof that it works |
| 2026-08-23 | The `/code-review` agent asserted that "PR #5 already carries review threads" and concluded the force-push window was closed, so `Squash and merge` was the only remedy for the branch's add-then-remove history. The agent's claim was repeated to the human as established fact without being checked. `gh` showed zero reviews and zero inline comments on PR #5 — the single comment is Codex's usage-limit notice, which is not a review. The human caught it by opening the PR | Process fixed, no code change. The window was open, which turns the branch-history question from a forced fallback into a deliberate choice — recorded in the row below. Rule of thumb from this: a subagent's *factual* claims about repo or remote state are findings to verify, not results to relay, and the check is usually one command. The same round also produced a finding whose second half was wrong (the Resolution log is block-structured, not chronological, so the row it called misplaced was not), which is the same lesson at file scale |
| 2026-08-23 | PR #5's branch introduces `ShowRefreshError`, a local `SnackbarHost`, its query-payload guard and its `imePadding()` and then deletes all of them within the same branch — the exact pattern CLAUDE.md's "a branch is clean when… nothing is added and then removed within the branch" forbids | Rule deliberately not applied, by explicit human decision, with the force-push window verified open (so this is a choice and not a fallback). Kept as a merge commit with its history intact: this branch is the project's clearest worked example of the rule of three — patch, patch, patch, then redesign — and the diagnosis lives in the commit messages themselves. Squashing would delete the most instructive history in the repo to satisfy a rule whose purpose is readable history. Recorded here rather than in CLAUDE.md because it is a decision about one branch, not a new rule: the cleanliness rule stands, and the next branch does not inherit this exception |
| 2026-08-23 | Third `/code-review` round on PR #5 found two more ways the refresh snackbar drifted from what it described: a later *successful* refresh left the failure message sitting over fresh results (dismissal was keyed on the query, which had not changed), and an effect queued while the screen was disposed replayed on return, which the query-equality guard could not detect. Third touch of the same logic for the same class of defect, which is what the rule of three exists to catch | **Redesigned rather than patched a fourth time**, per the rule of three and an explicit human decision. Root cause named: a *persistent condition* was being announced by a *transient mechanism* (a 10-second snackbar), so their lifetimes could only ever drift — each guard closed one way and left the others. The distinction, and why this does not violate the MVI rule against one-shot events in `UiState`: [ADR 0017](decisions/0017-a-condition-is-not-an-event.md). `ShowRefreshError` is gone from `SearchUiEffect`; `SearchUiState.refreshError` drives an inline banner that the ViewModel clears on a successful load or a query change. The `SnackbarHost`, the `Channel` round trip, the query guard and the dismissal all disappeared with it. Verified on emulator-5554 with the network down; banner uses `errorContainer`/`onErrorContainer` from DESIGN.md, so no design drift. Also fixed the inset finding the human had independently spotted: `GameStackNavHost` now calls `consumeWindowInsets(innerPadding)`, since `padding()` alone does not mark insets handled and every inner `imePadding()` was counting the bottom bar twice |
| 2026-08-23 | Second `/code-review` pass on PR #5: dismissing the snackbar on a query change only reached one already *visible*. The effect can still be sitting in the rendezvous `Channel` — `onFailure` has no suspension point after the UseCase returns, so `mapLatest` cannot cancel it — and `currentSnackbarData` is then null, so a message about abandoned text appeared over the new query's skeleton with a Retry that refreshed the new query. Separately, the `SnackbarHost` had no `imePadding()`: `enableEdgeToEdge()` means `adjustResize` never shrinks the window, so it rendered behind the keyboard for its full 10-second duration, exactly as the message states already accounted for | Code fixed. `ShowRefreshError` now carries the effective query it belongs to and the screen drops it at *display* time if the query has moved on — the dismissal alone could only ever handle the already-visible case, so the guard had to move to where the message is shown. Dismissal rekeyed on the trimmed query, matching the ViewModel, so a trailing space no longer disturbs a snackbar that still applies. `imePadding()` added to the host, verified on emulator-5554 with the keyboard raised over a live snackbar. Note the untestable half: the staleness check lives in a Composable, so only the effect's payload is unit-tested — the drop itself waits on instrumented tests |
| 2026-08-23 | `/code-review` on PR #5 found the snackbar fix reintroducing the failure it was written to prevent. `games` was never cleared when a new query started, so it could hold results for text the user had moved on from; the error card's Retry emits `OnRefresh`, so `keepResults = isRefresh && games.isNotEmpty()` classified a retried *new* search as a refresh and restored the previous query's results under the new text. Third defect in a row traced to state outliving what it describes, after `lastSearchedQuery` and `searchJobQuery` | Code fixed at the root rather than at the symptom: `games` is cleared the moment the effective query changes, so results on screen always belong to the current query. `keepResults` then needs no `isRefresh` guard at all — having results *means* the query was already answered. Also fixed alongside: the snackbar outlived the state it described, staying up over a cleared field where its Retry silently did nothing, so it is now dismissed whenever the query changes. Regression test verified to fail without the fix, and the existing "no results" test now also asserts no effect is emitted |
| 2026-08-23 | Nothing recorded how review findings get triaged, when a review loop is allowed to stop, or what happens when the automated reviewer is unavailable. In practice PR #3 ran five bot rounds plus one `/code-review` in a day, hit the Codex quota, and stayed open long enough to accumulate unrelated work. Nothing recorded the habit that made its regression tests worth anything either — verifying each one fails against the code it guards | Doc updated — the triage, the stopping rule, the demotion of automated review to advisor, the rule of three and the regression-verification rule are [ADR 0015](decisions/0015-review-findings-are-triaged-and-three-is-the-limit.md); the `git stash` recipe and its failure modes went to `write-tests`. Block 4 gained it as a third deliverable, ranked above the drift checker, with the reasons a naive `PostToolUse` hook is the wrong shape and a note that the rule survives the hook rather than being replaced by it |
| 2026-08-23 | `SearchScreen` evaluated `errorMessage != null` as its first branch, so a failed pull-to-refresh replaced a full grid of loaded results with the error card — contradicting CLAUDE.md's MVI section, which names "a refresh failing while stale content is still on screen" as the very reason `UiState` is a data class and not sealed. The state modelled it correctly; the UI rendered the fields as mutually exclusive. Deferred from PR #3 to issue #4 to keep an oversized PR from growing | Code fixed — a failed refresh with results loaded now keeps them and raises `ShowRefreshError` (a one-shot `UiEffect`, since the results stay and only the announcement is transient) with a Retry action. Distinction found by an existing test while implementing: this applies to a *refresh* only. A failed search for new text still shows the error state, because the results on screen answer the previous query and keeping them under a snackbar would misrepresent what was searched. Spec updated with both halves. Snackbar host is local to `SearchScreen` for now, with the promotion trigger written next to it: move it to `GameStackApp`'s `Scaffold` as soon as a second screen needs one. **Superseded 2026-08-23 (see the redesign row at the top of this table, and [ADR 0017](decisions/0017-a-condition-is-not-an-event.md)): `ShowRefreshError` and the snackbar host no longer exist — the failure is reported by `SearchUiState.refreshError` as an inline banner. The user-facing behaviour described here still holds; only the mechanism changed** |
| 2026-08-23 | `/code-review` on the refactored pipeline found that `.debounce {}` sitting upstream of `.mapLatest {}` held a new keystroke for its own 400ms before `mapLatest` saw it, so the previous request kept running and its response could still land — showing settled results for text already replaced, or flashing the full-screen error card, for up to the whole debounce window. The hand-rolled `Job` version this replaced did *not* have that hole: it cancelled synchronously on every keystroke. The `new-viewmodel` skill had already been written documenting the flawed shape | Code fixed — the wait moved inside `mapLatest` (`delay(request.debounceMillis)`), so a newer request cancels the running one first and only then waits; the `debounce` operator and its `FlowPreview` opt-in are gone. Skill corrected in the same commit, with the trap stated explicitly because both shapes read almost identically. Noted for the record: this is the second time a fix in this file introduced the next defect, which is what the rule-of-three heuristic is meant to catch earlier |
| 2026-08-23 | `MessageState` centred ~280dp of content in a non-scrollable `fillMaxSize` column while Search passed it `imePadding()`, so in landscape or with the keyboard raised it clipped symmetrically and pushed the CTA off-screen, unreachable | Code fixed — `verticalScroll(rememberScrollState())` added; the min height constraint survives it, so short content stays centred |
| 2026-08-23 | `isRefreshing` was only set once `performSearch` ran, a coroutine hop after the pull was released, and nothing reset it when typing superseded a refresh — `PullToRefreshBox` reads the flag synchronously, so the indicator retracted and re-appeared on every pull, then spun over the shimmer grid when the user typed | Code fixed — set synchronously in `onRefresh`, and cleared in the branch where a new query supersedes the refresh, since `mapLatest` cancels that coroutine before it can reset the flag itself |
| 2026-08-23 | `search_results_heading` existed in `strings.xml` but was referenced nowhere; the results grid has no heading | Code fixed — string deleted; the grid renders no heading. **Note, 2026-08-23:** accurate about the PNG, but `search.html` did contain both a heading and a Filters button, invisible under a `sticky` overlap. See the row at the top of this table |
| 2026-08-23 | A fourth Codex pass found a fourth defect in the same ~40 lines of `SearchViewModel`: returning to a previously successful query after a failed one took the no-op branch, which left the failed query's `errorMessage` on screen covering results that were still loaded. Three consecutive review rounds had each found a real defect in that logic, and two of the fixes had introduced the next one | Code fixed by replacing the logic rather than patching it a fourth time, per an explicit human decision to stop patching. The hand-rolled `Job` plus remembered-query fields are gone; input now flows through `distinctUntilChanged` (a whitespace-only edit is not a new query), `debounce` (a newer request drops what was pending, so a refresh supersedes an undispatched keystroke) and `mapLatest` (a newer search cancels the running one). No field records which query was already searched — every version that kept one eventually let it disagree with the screen, which was the root of three of the four defects. Documented in `new-viewmodel` so the next debounced screen is not hand-rolled. Accepted trade, stated in the test: reverting text to a previously searched query now re-runs that search, one extra request behind the debounce, in exchange for deleting the field |
| 2026-08-22 | Code review of PR #3 found six defects the `build`/`test`/`lint` gate passed over, three of them introduced by the very fixes made earlier that day: a dedup early-return that skipped cancelling a pending job (results for a superseded query painted over the current text), `isRefreshing` never reset when clearing the query mid-refresh, `errorMessage` outliving the next keystroke's debounce, "Retry" showing "No Results Found" during the retry request, a rendezvous `Channel` replaying a duplicated navigation effect on return, and a blank Search screen before any query | Code fixed with regression tests for each; the two that were also documentation gaps went both ways — the Spec now specifies Search's initial state, which no mockup had ever defined, and the effect-delivery rule became [ADR 0011](decisions/0011-the-effect-channel-queues.md) |
| 2026-08-22 | `project-scaffold` was deleted from `.claude/skills/` while CLAUDE.md, the Spec, `new-feature` and `new-hilt-module` still referenced it as invocable | Doc updated — deletion kept (a once-per-project Skill only adds a dead option to the listing), content archived at `docs/project/retired-skills/project-scaffold.md`, CLAUDE.md gained a "Retired skills" entry, and the remaining references were reworded to past tense. Note for the record: the original motivation was token cost, which is largely a non-issue — Claude Code loads only a Skill's name and description up front, so an unused Skill costs about one line; the real justification is keeping dead options out of the listing |
| 2026-08-22 | The same rationale for `try`/`catch` over `runCatching` was written out in full in two RepositoryImpls, and the navigation structure rationale was duplicated between CLAUDE.md and code comments — the exact duplication CLAUDE.md → Tier 3 forbids, guaranteeing the copies drift | Both — doc updated and code fixed. The rule moved to CLAUDE.md and `new-repository-impl`; the code comments shrank to one-line pointers. The `runCatching` ban and the "never both" placement rule: [ADR 0009](decisions/0009-never-runcatching-around-a-suspend-call.md) |
| 2026-08-22 | CLAUDE.md's Git workflow was the only section with no Tier markers, named a "build/tests/lint gate" it never defined, and said nothing about branching, untracked files, or PRs — gaps that produced a real near-miss (untracked `ApicalypseRequestBody.kt` would have been dropped by `git add -u`) and a whole feature left uncommitted | Doc updated — the section was rewritten. The rules that came out of it: [ADR 0006](decisions/0006-the-git-workflow-is-tiered.md) |
| 2026-08-22 | Neither CLAUDE.md nor the Spec said how the bottom nav back stack works, so `Detail` had been implemented as a top-level sibling of the tabs. It got swept into the tabs' `saveState`/`restoreState` without belonging to any tab: leaving a tab from a Detail and returning restored the Detail under no selected tab, and the workaround for it used `NavController.currentBackStack` — `@RestrictTo` API that made `./gradlew lint` fail with a `RestrictedApi` **Error** | Both — code fixed and docs updated. Code: the canonical `popUpTo(findStartDestination){saveState}` + `restoreState` idiom is restored and the restricted-API workaround is gone. Docs: the Spec's App Navigation section states the user-facing half (a tab restores where you left it, Detail included). Approved by the human as a plan-level choice. The structure and every intended back-stack behaviour: [ADR 0010](decisions/0010-one-nested-graph-per-tab.md) |
| 2026-08-22 | CLAUDE.md defined "done" as `build`/`test`/`lint` only, so a UI feature could be declared finished with no device check — and none was proposed until the human asked for one. Every UI defect this session (premature empty state, keyboard covering a button, clipped genre text) was invisible to that gate. The technique needed to run the check was also unrecorded: it lived only in one session's memory, where a fresh agent on another machine would have to rediscover it | Doc updated — CLAUDE.md → Commands gained a Tier 2 "Device verification" section, [ADR 0005](decisions/0005-ui-changes-are-verified-on-the-emulator.md). `docs/project/design/README.md` created for the mockup-render recipe and the screenshot-cost model — deliberately not in CLAUDE.md, which governs architectural rules, not tool invocations. Also recorded there: shrinking the AVD saves no tokens, since the long edge is clamped before billing |
| 2026-08-22 | Tier 1's blanket "never force-push a branch that has already been pushed" made squash-merge the only remedy for an untidy branch, which contradicted the merge-strategy rule adopted the same day (merge commit by default, precisely to keep `blame` and `bisect` granularity). The absolute rule was guarding against overwriting a collaborator's work — a risk that does not exist in a solo repo | Doc updated after explicit conversation, as Tier 1 requires. The rule that resulted: [ADR 0007](decisions/0007-the-force-push-window-closes-at-the-first-review.md), since superseded by [ADR 0014](decisions/0014-the-force-push-window-closes-at-the-merge.md) |
| 2026-08-22 | A second Codex review pass found two defects that the first pass's own fixes had introduced: unconditionally cancelling `searchJob` on every keystroke also killed the request already in flight for the same effective query (a trailing space during a first search discarded the IGDB response and dropped the screen to "No Results Found"), and `rememberSingleClick` had been applied to the result cards only, leaving the empty-state "Go To Library" CTA able to queue a duplicate navigation effect | Code fixed with two regression tests, both verified to fail without the fix. `SearchViewModel` now tracks `searchJobQuery` — the effective query the active job serves — because "a job is already searching this exact text, let it land" and "a job is scheduled for text the user abandoned, cancel it" reach the same branch and need opposite handling; `cancelSearch`/`startSearch` extracted so refresh and debounce share one path. A no-argument `rememberSingleClick` overload added so single controls do not read as `rememberSingleClick<Unit>`. The rewording this forced — the guard applies to *every* navigation callback and is audited per screen, not per list — is part of [ADR 0011](decisions/0011-the-effect-channel-queues.md). Note: the CTA fix has no unit test, since `rememberSingleClick` is a Composable and instrumented tests remain deferred |
| 2026-08-22 | The Git workflow said nothing about merge strategy, PR size, or how bot review threads get closed, and "one branch per unit of work" had no enforcement point — PR #3 reached 1,992 additions across 46 files with only two commits belonging to the Search feature, and a Codex review thread stayed open on a finding that had already been fixed | Doc updated, and `.github/pull_request_template.md` created so the screenshot drop point is signposted automatically. The merge-strategy, PR-size and thread-closing rules: [ADR 0008](decisions/0008-merge-commit-by-default.md) |
| 2026-08-22 | `docs/project/design/search/search-initial.png` was a hand-taken screenshot of its HTML, reproducible by nobody — window size, zoom and OS scaling were recorded nowhere | Asset fixed — regenerated headlessly at 600×1775 from the HTML beside it; `design/README.md` now carries the command so the next one is reproducible |
| 2026-08-06 | `write-tests` and `new-mapper` claimed the write-path mapper round trip (`toEntity().toDomain()`) "proves TypeConverters line up" — false: those functions never call Room's registered `@TypeConverter`, so a wrong/unregistered converter would pass every existing test (found via Codex PR review on #2) | Doc updated — claim corrected in both Skills; added a direct, JVM-only `@TypeConverter` test (round trip + fallback) as its own coverage step in `new-room-dao` and `write-tests`, closing the actual gap instead of just disclosing it |
| 2026-08-06 | `new-hilt-module`'s "Current modules" table listed `DatabaseModule` as already existing; no `core/data/di/DatabaseModule.kt` exists in the repo (found via Codex PR review on #2) | Doc updated — marked `DatabaseModule (PENDING — not yet created)` per CLAUDE.md's Documentation completeness rule, with a note to create it on the first Room-backed binding |
| 2026-08-06 | Building the Search feature required deciding whether result cards show genre/developer per game — a product-level detail the Spec's one-line MVP scope for Search never recorded, though the approved mockup already showed it | Doc updated — Spec's Search section now states result cards show cover/name/genre/developer, sourced from IGDB `genres`/`involved_companies`, and distinguishes this from the still-backlog search filters |
| 2026-08-05 | `Type.kt` implemented 7 of the 9 token→role rows DESIGN.md now defines — `titleMedium`/`titleSmall` were missing, so `title-md`/`title-sm` (the card-title styles) silently fell back to M3 defaults in Inter rather than Hanken Grotesk | Code fixed — both roles added with DESIGN.md's values |
| 2026-08-05 | `AuthRepositoryImpl.getValidAccessToken()` chained `runCatching{}.map{}.also{}` to perform the cache assignment — the exact shape CLAUDE.md → Code Conventions → Tier 3 names ("a chain performing a side effect, e.g. a cache assignment") | Code fixed — cache write extracted to a named `token` variable and a plain assignment |
| 2026-08-05 | Hilt visibility rule ("keep it private if nothing outside the module injects it") collided with the `@Singleton` rule: applied literally to `NetworkModule`'s OkHttp/Retrofit providers it would have created one client per consumer, since scope applies to bindings and not to functions | Doc updated — code was right; `new-hilt-module` and CLAUDE.md were corrected. The carve-out and the dividing line it rests on: [ADR 0004](decisions/0004-hilt-bindings-stay-private-unless-singleton.md) |
| 2026-08-04 | Checklist demanded Light theme support, contradicting Tier 1 dark-only | Doc updated — item inverted into a violation detector for Light/dynamic color |
| 2026-08-04 | A decision with no incident behind it: the write-path conversion had no defined home | Recorded as [ADR 0003](decisions/0003-dto-and-entity-types-never-leave-the-data-layer.md). The row is kept as a stub so the timeline stays complete |
| 2026-08-04 | A decision with no incident behind it: the `completedAt` transition rule fit in no layer without breaking one | Recorded as [ADR 0002](decisions/0002-the-repository-owns-the-timestamp-writes.md). The row is kept as a stub so the timeline stays complete |
| 2026-08-04 | DESIGN.md asked for dynamic color; code and brand identity forbid it | Doc updated — code was right (`Theme.kt` never had it), and DESIGN.md, the Spec and CLAUDE.md were brought into line with it. The rule itself: [ADR 0001](decisions/0001-no-dynamic-color.md) |
| 2026-08-04 | DESIGN.md prose specified white button labels; `on-primary` is #3c0091 | Doc updated — prose corrected to use the `on-*` role |
| 2026-08-04 | Skills referenced undefined concepts (Human-in-the-Loop, MVI contracts, Block 4, Self-Healing Loop, `discovery` skill) | Doc updated — all four defined in CLAUDE.md/Spec; the fifth reference removed |
| 2026-08-04 | Room/DAO, Hilt modules and interceptors had no owning Skill | Skills added — `new-room-dao`, `new-hilt-module` |
| 2026-08-04 | Type/Shape/unit mappings decided in code but recorded nowhere | Doc updated — DESIGN.md now carries both mapping tables and the rem→dp rule |
